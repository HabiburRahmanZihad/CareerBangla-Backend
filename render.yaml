services:
  - type: web
    name: careerbangla-backend
    runtime: node
    buildCommand: pnpm install --frozen-lockfile && pnpm generate && pnpm build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
